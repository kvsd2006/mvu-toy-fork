# 01-动态肉体档案 — 设计摘要

> 完整设计文档（spec）：`E:\test\MVU规则更新\docs\superpowers\specs\2026-08-11-动态肉体档案-design.md`
>
> 本文档是该 spec 的精简摘要，仅含实施所需的关键信息。

## 设计决策

| 决策点 | 选择 |
|---|---|
| 数据流 | AI 双向（既写也读） |
| 字段粒度 | 结构化分项 + `record<string,string>` |
| 复用策略 | 最小补丁（3 处卡内位置，含 1 段类型声明 + 1 处外部 fork） |
| 内容定位 | 纯外观展示 + NSFW 语义参考，不参与战斗 |

## 改动清单

| # | 类型 | 位置 | 文件 |
|---|---|---|---|
| 1 | 卡内 | ZOD schema `角色`（V3.5.x-：`主角`）内部追加 `动态肉体档案` | `patches/01-zod-schema.md` |
| 2 | 卡内 | `[mvu_update]变量更新规则` 末尾追加维护规则 | `patches/02-mvu-update-rules.md` |
| 2b | 卡内 | 同条目 `角色:` 类型块内插 `动态肉体档案: &body_archive` 声明（V3.5.6 起） | `patches/02-mvu-update-rules.md` 末节 |
| 3 | 卡内 | `[variables]当前变量` EJS `current.角色` 块追加字段 | `patches/03-current-variables-ejs.md` |
| 4 | 外部 | 悬浮球 JS `renderInfoTab` 追加 secBlock | `patches/04-suspension-ball-js.md` |

> ⚠️ V3.6.11 起变量根节点 `主角` → `角色`；本表已按新名。迁移旧卡时把 `角色` 读回 `主角`。

## 数据模型

```
角色.动态肉体档案
  ├─ 肉体外观 : record<string,string>  （身高·体重 / 三围·罩杯 / 外貌）
  ├─ 衣物服饰 : record<string,string>  （上装/下装/内衣/内裤/袜子/鞋履/包袋）
  ├─ 情趣配饰 : record<string,string>  （17 个解剖学部位 key）
  └─ 性器状态 : record<string,string>  （9 个情色化部位 key）
```

## 部署步骤

1. 在角色卡 JSON 应用 3 处卡内补丁（按 `patches/01/02/03`）
2. 修改角色卡 `extensions.tavern_helper.scripts[]` 里的 `name: "悬浮球状态栏"` 脚本 content：
   ```
   import 'https://testingcf.jsdelivr.net/gh/kvsd2006/mvu-toy-fork@main/01-body-archive/dist/V20260917/suspension-ball.js?v=2'
   ```
3. SillyTavern 加载角色卡 → 信息 Tab → 验证「动态肉体档案」区块

## 升级工作流

| 场景 | 操作 |
|---|---|
| 改 UI | 改 `dist/V20260917/suspension-ball.js` → commit → push → `?v=N` 递增 |
| 上游发新 JS / 新卡 | 取基底（import 型卡→上游原版；内联型卡→卡内联）→ 按 `patches/04` 插入两段 → node --check → push |
| 上游发新卡 | 重新粘卡内补丁（01 / 02 / 02b / 03）；卡内 import 指向新 dist |

## 验证

```bash
node --check dist/V20260917/suspension-ball.js   # 语法校验
sha256sum dist/V20260917/suspension-ball.js      # 确认不同于上游（当前 = 7936c4a9…）
```
