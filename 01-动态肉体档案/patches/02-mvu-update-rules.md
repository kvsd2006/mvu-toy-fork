# 卡内补丁 #2：`[mvu_update]变量更新规则` 追加段

## 位置

角色卡世界书条目 `[mvu_update]变量更新规则`（卡内 930–974 行）的**末尾**追加 1 小节。

## 锚点稳定性

这个条目是协议层描述，原作者迭代频率低。末尾追加不修改原文任何一行。

## 补丁全文

```markdown
### 动态肉体档案维护规则（专用）

**用途定位**：`主角.动态肉体档案` 是**纯外观 + NSFW 语义**的快速参考，**不参与任何战斗/属性/EP/HP 计算**。作用是让 AI 一眼看到主角当前的肉体状态、穿着、配饰、性器状态，用于：
- 在剧情里准确描写主角的样子（基于档案填表，不要凭空想象）
- 在 NSFW 场景里合理利用现有配饰（如跳蛋、贞操带、颈圈等）和当前性器状态
- 让身体反应（兴奋度、敏感、伤痕）具有跨回合连续性

**字段结构**：
- `肉体外观`：`身高·体重`（例：160cm/45kg）、`三围·罩杯`（例：96/52/88 G）、`外貌`（短语概括）
- `衣物服饰`：`上装`、`下装`、`内衣`、`内裤`、`袜子`、`鞋履`、`包袋`（单肩/双肩/手提包统一放此槽位）
- `情趣配饰`（解剖学部位 key）：`身体`、`头部`、`耳部`、`口部`、`颈部`、`胸部`、`腰部`、`腹部`、`臀部`、`手部`、`腿部`、`脚部`、`外阴`、`阴道`、`子宫`、`肛部`、`尿道`
- `性器状态`（情色化部位 key）：`口穴`、`酥胸`、`牝户`（对应外阴）、`雌穴`（对应阴道）、`子宫`、`屁穴`（对应肛部）、`俏手`、`美腿`、`玉足`

**填表风格约束**：
- 每个字段值用**短语或关键词**，禁止整段描述
  - ✓ `"黑色蕾丝胸罩"`  ✗ `"她穿着一件精美的黑色蕾丝胸罩..."`
- 字段不再适用时用 `remove` 删，不要写"无"或"暂无"

**维护时机**（必须触发 mvu_update）：
1. **剧情变化**：穿脱衣物、TF/性别变化、伤痕、佩戴/取下配饰、性器状态变化（兴奋/高潮/疲倦）
2. **OOC/元指令**：玩家用括号或 OOC 明确要求更新（如 `(把衣物更新为黑色旗袍)`、`OOC: 性器状态改为兴奋`）
3. **NSFW 场景开始与中段**：进入性场景时主动同步性器状态、敏感部位、配饰使用情况

**更新方式**（关键：容器不存在则先建容器，再逐槽位填）：
- **判断容器是否存在**：查看当前 `<status_current_variables>` 中是否有 `动态肉体档案` 字段
  - 若**容器不存在**（首次初始化）：第一条操作必须是 `replace /主角/动态肉体档案 {"肉体外观":{},"衣物服饰":{},"情趣配饰":{},"性器状态":{}}`，先建立空容器。此操作仅首次执行一次
  - 若**容器已存在**：直接逐槽位 `replace`（如下），禁止覆盖整个档案对象
- `replace /主角/动态肉体档案/<子档案>/<具体槽位> "<新值>"` — 更新或创建单槽位
- `remove /主角/动态肉体档案/<子档案>/<槽位>` — 删除槽位
- **禁止**在容器已存在的情况下用单个 `replace` 覆盖整个子档案或整个档案对象（会清空其他槽位）
- **每次回复中，对需要更新的所有槽位逐槽位写 `replace`，一条 `replace` 只改一个槽位**

**槽位自由扩展**：四个 record 内部字段不固定。AI 可根据剧情需要新增任意 key（如 `性器状态.后庭`、`情趣配饰.耳饰·左`），key 名要简洁、与现有命名风格一致。

**配饰-状态联动（重要）**：情趣配饰与性器状态存在解剖学映射关系，必须成对维护：
- `外阴`→`牝户`、`阴道`→`雌穴`、`肛部`→`屁穴`、`子宫`→`子宫`
- 当更新某个情趣配饰槽位时，**必须同步更新对应性器状态槽位**（不要留空）
- 反之亦然：性器状态变化时，检查对应配饰是否也应同步更新

**⚡优先级提醒**：剧情涉及肉体/衣物/配饰/性器变化时，本档案的 mvu_update 与其他系统（HP/状态/装备）同等优先级，**不可遗漏**。
```
